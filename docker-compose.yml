services:
  db:
    image: postgres
    ports:
      - "15432:5432"
    environment:
      - POSTGRES_PASSWORD=admin
    volumes:
      # creates the initial db and user
      - "./database/mount:/docker-entrypoint-initdb.d"
      # saves data between docker runs
      - "./database/data:/var/lib/postgresql/data"