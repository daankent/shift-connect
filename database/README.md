# ShiftConnect Database

The database runs in a Docker container, and the data is stored in a volume that is mounted to the host system. This
means that if you destroy your database container, the actual data will remain intact.